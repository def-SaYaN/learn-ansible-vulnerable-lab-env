---
# Ansible Galaxy collections required to build the lab.
# Install with:  ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.windows
    version: ">=2.5.0"
  - name: community.windows
    version: ">=2.3.0"
  - name: microsoft.ad
    version: ">=1.7.0"
  - name: community.general
    version: ">=9.0.0"
  - name: ansible.utils
    version: ">=5.0.0"
